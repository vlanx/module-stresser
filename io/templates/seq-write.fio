[global]
direct=1
time_based=1
runtime=${RUNTIME_SEC}
group_reporting=1

iodepth=${IODEPTH}
numjobs=${NUMJOBS}

directory=${TEST_DIR}
filename=${FILENAME}
size=${FILE_SIZE_GB}G

[seq-write]
rw=write
bs=${BLOCK_SIZE}
overwrite=1
buffer_pattern=${PATTERN}
