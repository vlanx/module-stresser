apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  name: master-workflow
  namespace: stress
spec:
  serviceAccountName: stress-sa # For proper permissions
  ttlStrategy:
    secondsAfterCompletion: 300 # Delete the job file after 300s
    secondsAfterSuccess: 300     
    secondsAfterFailure: 3600   # 1h
  entrypoint: suite
  
  # Reusable cooldown gate lives here, not in the module
  templates:
  - name: cooldown
    suspend:
      duration: 5m

  - name: master
    steps:
### CPU ###
    - - name: cpu-24w-20pct-10m
        templateRef:
          name: cpu-stress      # WorkflowTemplate name (same namespace)
          template: cpu-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/cpu-stress:latest }
          - { name: workers, value: "24" }
          - { name: load,    value: "20" }
          - { name: timeout, value: "10m" }
          - { name: cpuset,  value: "0-23" }
          - { name: display, value: "CPU-24w-20pct-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "24" }
          - { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "1Gi" }

    - - name: cooldown-1cpu
        template: cooldown

    - - name: cpu-24w-40pct-10m
        templateRef:
          name: cpu-stress      # WorkflowTemplate name (same namespace)
          template: cpu-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/cpu-stress:latest }
          - { name: workers, value: "24" }
          - { name: load,    value: "40" }
          - { name: timeout, value: "10m" }
          - { name: cpuset,  value: "0-23" }
          - { name: display, value: "CPU-24w-40pct-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "24" }
          - { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "1Gi" }

    - - name: cooldown-2cpu
        template: cooldown

    - - name: cpu-24w-60pct-10m
        templateRef:
          name: cpu-stress      # WorkflowTemplate name (same namespace)
          template: cpu-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/cpu-stress:latest }
          - { name: workers, value: "24" }
          - { name: load,    value: "60" }
          - { name: timeout, value: "10m" }
          - { name: cpuset,  value: "0-23" }
          - { name: display, value: "CPU-24w-60pct-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "24" }
          - { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "1Gi" }

    - - name: cooldown-3cpu
        template: cooldown

    - - name: cpu-24w-80pct-10m
        templateRef:
          name: cpu-stress      # WorkflowTemplate name (same namespace)
          template: cpu-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/cpu-stress:latest }
          - { name: workers, value: "24" }
          - { name: load,    value: "80" }
          - { name: timeout, value: "10m" }
          - { name: cpuset,  value: "0-23" }
          - { name: display, value: "CPU-1w-80pct-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "24" }
          - { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "1Gi" }

    - - name: cooldown-4cpu
        template: cooldown

    - - name: cpu-24w-100pct-10m
        templateRef:
          name: cpu-stress      # WorkflowTemplate name (same namespace)
          template: cpu-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/cpu-stress:latest }
          - { name: workers, value: "24" }
          - { name: load,    value: "100" }
          - { name: timeout, value: "10m" }
          - { name: cpuset,  value: "0-23" }
          - { name: display, value: "CPU-24w-100pct-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "24" }
          - { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "1Gi" }


    - - name: cooldown-cpu2mem
        template: cooldown

### MEM ###
    - - name: memory-12G-10m
        templateRef:
          name: memory-stress      # WorkflowTemplate name (same namespace)
          template: memory-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/memory-stress:latest }
          - { name: workers, value: "24" }
          - { name: read_mbps,    value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: write_mbps, value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: timeout,  value: "10m" }
          - { name: size,  value: "0.5" } # Size in Gb for the memory test
          - { name: memrate_flush,  value: true } 
          - { name: memrate_method,  value: "all" } 
          - { name: cpuset, value: "0-23" }
          - { name: display, value: "memory-12G-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          #- { name: cpu_req, value: "24" }
          #- { name: cpu_lim, value: "24" }
            #- { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "64Gi" }

    - - name: cooldown-1mem
        template: cooldown

    - - name: memory-24G-10m
        templateRef:
          name: memory-stress      # WorkflowTemplate name (same namespace)
          template: memory-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/memory-stress:latest }
          - { name: workers, value: "24" }
          - { name: read_mbps,    value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: write_mbps, value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: timeout,  value: "10m" }
          - { name: size,  value: "1" } # Size in Gb for the memory test
          - { name: memrate_flush,  value: true } 
          - { name: memrate_method,  value: "all" } 
          - { name: cpuset, value: "0-23" }
          - { name: display, value: "memory-24G-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          #- { name: cpu_req, value: "24" }
          #- { name: cpu_lim, value: "24" }
            #- { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "64Gi" }

    - - name: cooldown-2mem
        template: cooldown

    - - name: memory-36G-10m
        templateRef:
          name: memory-stress      # WorkflowTemplate name (same namespace)
          template: memory-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/memory-stress:latest }
          - { name: workers, value: "24" }
          - { name: read_mbps,    value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: write_mbps, value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: timeout,  value: "10m" }
          - { name: size,  value: "1.5" } # Size in Gb for the memory test
          - { name: memrate_flush,  value: true } 
          - { name: memrate_method,  value: "all" } 
          - { name: cpuset, value: "0-23" }
          - { name: display, value: "memory-36G-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          #- { name: cpu_req, value: "24" }
          #- { name: cpu_lim, value: "24" }
            #- { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "64Gi" }

    - - name: cooldown-3mem
        template: cooldown

    - - name: memory-48G-10m
        templateRef:
          name: memory-stress      # WorkflowTemplate name (same namespace)
          template: memory-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/memory-stress:latest }
          - { name: workers, value: "24" }
          - { name: read_mbps,    value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: write_mbps, value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: timeout,  value: "10m" }
          - { name: size,  value: "2" } # Size in Gb for the memory test
          - { name: memrate_flush,  value: true } 
          - { name: memrate_method,  value: "all" } 
          - { name: cpuset, value: "0-23" }
          - { name: display, value: "memory-48G-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          #- { name: cpu_req, value: "24" }
          #- { name: cpu_lim, value: "24" }
            #- { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "64Gi" }

    - - name: cooldown-4mem
        template: cooldown

    - - name: memory-58G-10m
        templateRef:
          name: memory-stress      # WorkflowTemplate name (same namespace)
          template: memory-stress-container         # inner template name
        arguments:
          parameters:
          - { name: image,   value: ghcr.io/vlanx/memory-stress:latest }
          - { name: workers, value: "24" }
          - { name: read_mbps,    value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: write_mbps, value: "-1" } # Value -1 disables cap on read speed. Value 0 disables reads
          - { name: timeout,  value: "10m" }
          - { name: size,  value: "2.4" } # Size in Gb for the memory test
          - { name: memrate_flush,  value: true } 
          - { name: memrate_method,  value: "all" } 
          - { name: cpuset, value: "0-23" }
          - { name: display, value: "memory-58G-10m" }
          ### CPU Requests/Limits for when we want more workers/cores
          #- { name: cpu_req, value: "24" }
          #- { name: cpu_lim, value: "24" }
            #- { name: mem_req, value: "1Gi" }
          - { name: mem_lim, value: "64Gi" }

    - - name: cooldown-mem2io
        template: cooldown

### IO ###
    - - name: seq-write-12g-10m-55mbps
        templateRef:
          name: io-stress
          template: io-stress-container
        arguments:
          parameters:
          - { name: image,         value: ghcr.io/vlanx/io-stress:latest }
          - { name: mode,          value: "seq-write" }
          - { name: runtime_sec,   value: "600" }
          - { name: numjobs,       value: "1" }
            #- { name: file_size_gb,  value: "12" }
          - { name: test_dir,      value: "/scratch" }
          - { name: filename,      value: "fio_rand.dat" }
          - { name: block_size,    value: "1M" }
          - { name: iodepth,       value: "64" }
          - { name: target_mbps,   value: "55" } # Best when its sequential
          #- { name: target_iops,   value: "50" }
          - { name: host_path,     value: "/var/lib/module-stresser" }
          - { name: display,       value: "io-seq-write-12g-10m-55mbps" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "1" }
          - { name: mem_req, value: "512Mi" }
          - { name: mem_lim, value: "512Mi" }

    - - name: cooldown-1io
        template: cooldown

    - - name: seq-write-12g-10m-110mbps
        templateRef:
          name: io-stress
          template: io-stress-container
        arguments:
          parameters:
          - { name: image,         value: ghcr.io/vlanx/io-stress:latest }
          - { name: mode,          value: "seq-write" }
          - { name: runtime_sec,   value: "600" }
          - { name: numjobs,       value: "1" }
            #- { name: file_size_gb,  value: "12" }
          - { name: test_dir,      value: "/scratch" }
          - { name: filename,      value: "fio_rand.dat" }
          - { name: block_size,    value: "1M" }
          - { name: iodepth,       value: "64" }
          - { name: target_mbps,   value: "110" } # Best when its sequential
          #- { name: target_iops,   value: "50" }
          - { name: host_path,     value: "/var/lib/module-stresser" }
          - { name: display,       value: "io-seq-write-12g-10m-110mbps" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "1" }
          - { name: mem_req, value: "512Mi" }
          - { name: mem_lim, value: "512Mi" }

    - - name: cooldown-2io
        template: cooldown

    - - name: seq-write-12g-10m-166mbps
        templateRef:
          name: io-stress
          template: io-stress-container
        arguments:
          parameters:
          - { name: image,         value: ghcr.io/vlanx/io-stress:latest }
          - { name: mode,          value: "seq-write" }
          - { name: runtime_sec,   value: "600" }
          - { name: numjobs,       value: "1" }
            #- { name: file_size_gb,  value: "12" }
          - { name: test_dir,      value: "/scratch" }
          - { name: filename,      value: "fio_rand.dat" }
          - { name: block_size,    value: "1M" }
          - { name: iodepth,       value: "64" }
          - { name: target_mbps,   value: "166" } # Best when its sequential
          #- { name: target_iops,   value: "50" }
          - { name: host_path,     value: "/var/lib/module-stresser" }
          - { name: display,       value: "io-seq-write-12g-10m-166mbps" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "1" }
          - { name: mem_req, value: "512Mi" }
          - { name: mem_lim, value: "512Mi" }

    - - name: cooldown-3io
        template: cooldown

    - - name: seq-write-12g-10m-221mbps
        templateRef:
          name: io-stress
          template: io-stress-container
        arguments:
          parameters:
          - { name: image,         value: ghcr.io/vlanx/io-stress:latest }
          - { name: mode,          value: "seq-write" }
          - { name: runtime_sec,   value: "600" }
          - { name: numjobs,       value: "1" }
            #- { name: file_size_gb,  value: "12" }
          - { name: test_dir,      value: "/scratch" }
          - { name: filename,      value: "fio_rand.dat" }
          - { name: block_size,    value: "1M" }
          - { name: iodepth,       value: "64" }
          - { name: target_mbps,   value: "221" } # Best when its sequential
          #- { name: target_iops,   value: "50" }
          - { name: host_path,     value: "/var/lib/module-stresser" }
          - { name: display,       value: "io-seq-write-12g-10m-221mbps" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "1" }
          - { name: mem_req, value: "512Mi" }
          - { name: mem_lim, value: "512Mi" }

    - - name: cooldown-4io
        template: cooldown

    - - name: rand-read-12g-10m-276mbps
        templateRef:
          name: io-stress
          template: io-stress-container
        arguments:
          parameters:
          - { name: image,         value: ghcr.io/vlanx/io-stress:latest }
          - { name: mode,          value: "seq-write" }
          - { name: runtime_sec,   value: "600" }
          - { name: numjobs,       value: "1" }
            #- { name: file_size_gb,  value: "12" }
          - { name: test_dir,      value: "/scratch" }
          - { name: filename,      value: "fio_rand.dat" }
          - { name: block_size,    value: "1M" }
          - { name: iodepth,       value: "64" }
          - { name: target_mbps,   value: "276" } # Best when its sequential
          #- { name: target_iops,   value: "50" }
          - { name: host_path,     value: "/var/lib/module-stresser" }
          - { name: display,       value: "io-seq-write-12g-10m-276mbps" }
          ### CPU Requests/Limits for when we want more workers/cores
          - { name: cpu_req, value: "1" }
          - { name: cpu_lim, value: "1" }
          - { name: mem_req, value: "512Mi" }
          - { name: mem_lim, value: "512Mi" }

    - - name: cooldown-io2net
        template: cooldown

### NET ###
    - - name: tcp-upload-2000mbit-600s
        templateRef:
          name: network-stress
          template: network-stress-container
        arguments:
          parameters:
            - { name: image,     value: ghcr.io/vlanx/network-stress:latest }
            - { name: display,   value: "net-tcp-ul-2000mbit-600s" }
            - { name: server,    value: "xbox.av.it.pt" }   # <— your server
            - { name: proto,     value: "tcp" }
            - { name: direction, value: "upload" }
            - { name: bandwidth, value: "2000" }            # Mbit/s
            - { name: time,      value: "600" }
            - { name: parallel,  value: "1" }
            - { name: zerocopy,  value: "1" }
            # resources
            - { name: cpu_req, value: "1" }
            - { name: cpu_lim, value: "1" }
            - { name: mem_req, value: "256Mi" }
            - { name: mem_lim, value: "256Mi" }

    - - name: cooldown-1net
        template: cooldown

    - - name: tcp-upload-4000mbit-600s
        templateRef:
          name: network-stress
          template: network-stress-container
        arguments:
          parameters:
            - { name: image,     value: ghcr.io/vlanx/network-stress:latest }
            - { name: display,   value: "net-tcp-ul-4000mbit-600s" }
            - { name: server,    value: "xbox.av.it.pt" }   # <— your server
            - { name: proto,     value: "tcp" }
            - { name: direction, value: "upload" }
            - { name: bandwidth, value: "4000" }            # Mbit/s
            - { name: time,      value: "600" }
            - { name: parallel,  value: "1" }
            - { name: zerocopy,  value: "1" }
            # resources
            - { name: cpu_req, value: "1" }
            - { name: cpu_lim, value: "1" }
            - { name: mem_req, value: "256Mi" }
            - { name: mem_lim, value: "256Mi" }

    - - name: cooldown-2net
        template: cooldown

    - - name: tcp-upload-6000mbit-600s
        templateRef:
          name: network-stress
          template: network-stress-container
        arguments:
          parameters:
            - { name: image,     value: ghcr.io/vlanx/network-stress:latest }
            - { name: display,   value: "net-tcp-ul-6000mbit-600s" }
            - { name: server,    value: "xbox.av.it.pt" }   # <— your server
            - { name: proto,     value: "tcp" }
            - { name: direction, value: "upload" }
            - { name: bandwidth, value: "6000" }            # Mbit/s
            - { name: time,      value: "600" }
            - { name: parallel,  value: "1" }
            - { name: zerocopy,  value: "1" }
            # resources
            - { name: cpu_req, value: "1" }
            - { name: cpu_lim, value: "1" }
            - { name: mem_req, value: "256Mi" }
            - { name: mem_lim, value: "256Mi" }

    - - name: cooldown-3net
        template: cooldown

    - - name: tcp-upload-8000mbit-600s
        templateRef:
          name: network-stress
          template: network-stress-container
        arguments:
          parameters:
            - { name: image,     value: ghcr.io/vlanx/network-stress:latest }
            - { name: display,   value: "net-tcp-dl-8000mbit-600s" }
            - { name: server,    value: "xbox.av.it.pt" }   # <— your server
            - { name: proto,     value: "tcp" }
            - { name: direction, value: "upload" }
            - { name: bandwidth, value: "8000" }            # Mbit/s
            - { name: time,      value: "600" }
            - { name: parallel,  value: "1" }
            - { name: zerocopy,  value: "1" }
            # resources
            - { name: cpu_req, value: "1" }
            - { name: cpu_lim, value: "1" }
            - { name: mem_req, value: "256Mi" }
            - { name: mem_lim, value: "256Mi" }

    - - name: cooldown-4net
        template: cooldown

    - - name: tcp-upload-10000mbit-600s
        templateRef:
          name: network-stress
          template: network-stress-container
        arguments:
          parameters:
            - { name: image,     value: ghcr.io/vlanx/network-stress:latest }
            - { name: display,   value: "net-tcp-dl-10000mbit-600s" }
            - { name: server,    value: "xbox.av.it.pt" }   # <— your server
            - { name: proto,     value: "tcp" }
            - { name: direction, value: "upload" }
            - { name: bandwidth, value: "10000" }            # Mbit/s
            - { name: time,      value: "600" }
            - { name: parallel,  value: "1" }
            - { name: zerocopy,  value: "1" }
            # resources
            - { name: cpu_req, value: "1" }
            - { name: cpu_lim, value: "1" }
            - { name: mem_req, value: "256Mi" }
            - { name: mem_lim, value: "256Mi" }

    - - name: cooldown-net2app
        template: cooldown

### APP ###
    - - name: k6-qp-sine-5000vu
        templateRef:
          name: k6-runner          # WorkflowTemplate name
          template: k6-test-container  # inner template name
        arguments:
          parameters:
          - { name: image,            value: grafana/k6:latest }
          - { name: script_configmap, value: k6-sine-script }
          - { name: script_file,      value: script.js }

          - { name: BASE_URL,         value: "http://quickpizza-public-api.default.svc.cluster.local:3333" }
          - { name: DURATION,         value: "600" }   # seconds per cycle
          - { name: MAX_VUSERS,       value: "5000" }  # peak VUs multiplier

          - { name: k6_args,          value: "--verbose" }
          - { name: display,          value: "K6-600s-5000vu" }

          # Resource tuning (can adjust per step)
          - { name: cpu_req,          value: "24" }
          - { name: cpu_lim,          value: "24" }
          - { name: mem_req,          value: "8Gi" }
          - { name: mem_lim,          value: "32Gi" }

    - - name: cooldown-1app
        template: cooldown

    - - name: k6-qp-sine-10000vu
        templateRef:
          name: k6-runner          # WorkflowTemplate name
          template: k6-test-container  # inner template name
        arguments:
          parameters:
          - { name: image,            value: grafana/k6:latest }
          - { name: script_configmap, value: k6-sine-script }
          - { name: script_file,      value: script.js }

          - { name: BASE_URL,         value: "http://quickpizza-public-api.default.svc.cluster.local:3333" }
          - { name: DURATION,         value: "600" }   # seconds per cycle
          - { name: MAX_VUSERS,       value: "10000" }  # peak VUs multiplier

          - { name: k6_args,          value: "--verbose" }
          - { name: display,          value: "K6-600s-10000vu" }

          # Resource tuning (can adjust per step)
          - { name: cpu_req,          value: "24" }
          - { name: cpu_lim,          value: "24" }
          - { name: mem_req,          value: "8Gi" }
          - { name: mem_lim,          value: "32Gi" }

    - - name: cooldown-2app
        template: cooldown

    - - name: k6-qp-sine-15000vu
        templateRef:
          name: k6-runner          # WorkflowTemplate name
          template: k6-test-container  # inner template name
        arguments:
          parameters:
          - { name: image,            value: grafana/k6:latest }
          - { name: script_configmap, value: k6-sine-script }
          - { name: script_file,      value: script.js }

          - { name: BASE_URL,         value: "http://quickpizza-public-api.default.svc.cluster.local:3333" }
          - { name: DURATION,         value: "600" }   # seconds per cycle
          - { name: MAX_VUSERS,       value: "15000" }  # peak VUs multiplier

          - { name: k6_args,          value: "--verbose" }
          - { name: display,          value: "K6-600s-15000vu" }

          # Resource tuning (can adjust per step)
          - { name: cpu_req,          value: "24" }
          - { name: cpu_lim,          value: "24" }
          - { name: mem_req,          value: "8Gi" }
          - { name: mem_lim,          value: "32Gi" }

    - - name: cooldown-3app
        template: cooldown

    - - name: k6-qp-sine-20000vu
        templateRef:
          name: k6-runner          # WorkflowTemplate name
          template: k6-test-container  # inner template name
        arguments:
          parameters:
          - { name: image,            value: grafana/k6:latest }
          - { name: script_configmap, value: k6-sine-script }
          - { name: script_file,      value: script.js }

          - { name: BASE_URL,         value: "http://quickpizza-public-api.default.svc.cluster.local:3333" }
          - { name: DURATION,         value: "600" }   # seconds per cycle
          - { name: MAX_VUSERS,       value: "20000" }  # peak VUs multiplier

          - { name: k6_args,          value: "--verbose" }
          - { name: display,          value: "K6-600s-20000vu" }

          # Resource tuning (can adjust per step)
          - { name: cpu_req,          value: "24" }
          - { name: cpu_lim,          value: "24" }
          - { name: mem_req,          value: "8Gi" }
          - { name: mem_lim,          value: "32Gi" }

    - - name: cooldown-4app
        template: cooldown

    - - name: k6-qp-sine-25000vu
        templateRef:
          name: k6-runner          # WorkflowTemplate name
          template: k6-test-container  # inner template name
        arguments:
          parameters:
          - { name: image,            value: grafana/k6:latest }
          - { name: script_configmap, value: k6-sine-script }
          - { name: script_file,      value: script.js }

          - { name: BASE_URL,         value: "http://quickpizza-public-api.default.svc.cluster.local:3333" }
          - { name: DURATION,         value: "600" }   # seconds per cycle
          - { name: MAX_VUSERS,       value: "25000" }  # peak VUs multiplier

          - { name: k6_args,          value: "--verbose" }
          - { name: display,          value: "K6-600s-25000vu" }

          # Resource tuning (can adjust per step)
          - { name: cpu_req,          value: "24" }
          - { name: cpu_lim,          value: "24" }
          - { name: mem_req,          value: "8Gi" }
          - { name: mem_lim,          value: "32Gi" }
